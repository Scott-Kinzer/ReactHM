import React from 'react';
import loader from '../../img/loader.svg';

const Loader = () => {
    return (
        <div>
            <img src={loader} alt=""/>
        </div>
    );
};

export default Loader;
